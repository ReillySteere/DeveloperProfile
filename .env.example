# ===========================================
# Environment Configuration
# ===========================================
# Copy this file to .env and fill in values
#
# Quick start:
#   cp .env.example .env
#   # Edit .env with your values
#   npm run start

# ------------------------------------------
# Required
# ------------------------------------------

# JWT Authentication Secret
# Generate with: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_AUTH_SECRET=your-secret-key-here

# ------------------------------------------
# Optional
# ------------------------------------------

# Node environment (development | production | test)
NODE_ENV=development

# Server port (default: 3000)
PORT=3000

# Sentry DSN for error tracking
# In production, a default DSN is used if not set
# Set explicitly to override, or leave empty to use default (prod) or disable (dev)
# Get your DSN from: https://sentry.io/
SENTRY_DSN=

# Database path (default: data/database.sqlite)
# For in-memory testing, use :memory:
DATABASE_PATH=data/database.sqlite

# ------------------------------------------
# Alerting (Phase 2 - Optional)
# ------------------------------------------
# Email alerting configuration
# Required only if email alerts are enabled in alert rules
# See: architecture/decisions/ADR-013-rate-limiting-visualization.md

# Recipient email for alerts
ALERT_EMAIL_TO=

# SMTP server configuration
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
